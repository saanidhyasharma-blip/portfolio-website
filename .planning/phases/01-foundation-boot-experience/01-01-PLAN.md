---
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/lenis.ts
  - src/styles/globals.css
autonomous: true
---

# Plan: 01-01-PLAN.md - Engine Scaffolding

Initialize the Next.js 15 environment with the creative tech stack (GSAP, Three.js, Lenis, Tailwind) and establish the global layout.

## Context

We are building an immersive arcade-themed portfolio. This plan sets up the "Engine" (dependencies and global styles) so we can build the 3D and animation components in the next plans.

## Tasks

<task id="1">
<read_first>
- .planning/research/STACK.md
- .planning/research/ARCHITECTURE.md
</read_first>
<action>
Scaffold the Next.js project using `npx create-next-app@latest` in the current directory with TypeScript, Tailwind, and App Router.
</action>
<acceptance_criteria>
- `package.json` exists and includes `next`, `react`, and `react-dom`.
- `src/app/layout.tsx` and `src/app/page.tsx` exist.
- `tailwind.config.ts` exists.
</acceptance_criteria>
</task>

<task id="2">
<read_first>
- .planning/research/STACK.md
</read_first>
<action>
Install the core creative dependencies: `three`, `@types/three`, `@react-three/fiber`, `@react-three/drei`, `@react-three/postprocessing`, `gsap`, `framer-motion`, `lenis`, `zustand`, `lucide-react`, `clsx`, `tailwind-merge`.
</action>
<acceptance_criteria>
- `package.json` contains all specified dependencies.
- `node_modules` is populated (after install).
</acceptance_criteria>
</task>

<task id="3">
<read_first>
- src/app/layout.tsx
- .planning/research/ARCHITECTURE.md
</read_first>
<action>
Initialize Lenis smooth scrolling. Create `src/lib/lenis.ts` for the provider and wrap the layout in it.
</action>
<acceptance_criteria>
- `src/lib/lenis.ts` exists.
- `src/app/layout.tsx` imports and uses the Lenis provider.
</acceptance_criteria>
</task>

<task id="4">
<read_first>
- src/styles/globals.css
- .planning/PROJECT.md
</read_first>
<action>
Configure global styles in `src/styles/globals.css` with arcade-inspired defaults:
- Dark background (#050505).
- Neon accent colors as CSS variables.
- Custom cursor hiding (to be replaced by our custom one).
- Lenis required CSS for smooth scrolling.
</action>
<acceptance_criteria>
- `globals.css` contains `:root` variables for arcade colors.
- `globals.css` includes `html.lenis` styles.
- Body background is set to a near-black dark mode.
</acceptance_criteria>
</task>

## Verification Criteria

### Automated
- `npm run build` succeeds (scaffold check).
- `npx tsc` passes.

### Manual
- Inspect `layout.tsx` to ensure proper wrapping of providers.
- Verify `tailwind.config.ts` includes the correct content paths.

## Must Haves
- [ ] Next.js 15 App Router functional.
- [ ] All creative libraries (Three, GSAP, etc.) present in `package.json`.
- [ ] Lenis smooth scrolling scaffolded.
- [ ] Global dark theme active.
