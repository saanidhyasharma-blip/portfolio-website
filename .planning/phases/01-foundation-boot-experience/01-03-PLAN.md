---
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - src/components/arcade/CRTOverlay.tsx
  - src/app/layout.tsx
  - src/styles/globals.css
autonomous: true
---

# Plan: 01-03-PLAN.md - CRT Effects & Typography

Apply the "Arcade" visual layer globally via a CRT shader-inspired overlay and configure the futuristic typography system.

## Context

To achieve the "insanely creative" look, we need global scanlines and the correct futuristic fonts. This plan adds the "Visual Filter" that makes the whole site feel like a monitor.

## Tasks

<task id="1">
<read_first>
- .planning/research/STACK.md
- src/components/arcade/CRTOverlay.tsx
</read_first>
<action>
Create the `CRTOverlay` component in `src/components/arcade/CRTOverlay.tsx`.
- Use a CSS-based scanline overlay (repeating linear gradient).
- Add a subtle vignette and flicker animation (GSAP or CSS).
- Set `pointer-events: none` to ensure it doesn't block interactions.
</action>
<acceptance_criteria>
- `CRTOverlay.tsx` exists.
- Scanlines are visible when the component is rendered.
- Component does not block mouse clicks.
</acceptance_criteria>
</task>

<task id="2">
<read_first>
- src/app/layout.tsx
- src/components/arcade/CRTOverlay.tsx
</read_first>
<action>
Inject the `CRTOverlay` into the global `src/app/layout.tsx`.
- Wrap it around the main `{children}`.
</action>
<acceptance_criteria>
- `layout.tsx` imports and renders `CRTOverlay`.
- The effect is visible on all pages.
</acceptance_criteria>
</task>

<task id="3">
<read_first>
- src/app/layout.tsx
- src/styles/globals.css
</read_first>
<action>
Configure fonts in `src/app/layout.tsx` using `next/font/google`:
- **Orbitron** (Heading)
- **Press Start 2P** (Retro/Arcade accents)
- **Space Grotesk** (Body)
Set them as CSS variables in Tailwind.
</action>
<acceptance_criteria>
- Fonts are imported via `next/font/google`.
- Tailwind configuration includes the custom font families.
- Headings use Orbitron.
</acceptance_criteria>
</task>

## Verification Criteria

### Visual
- Scanlines are clearly visible but subtle enough not to strain eyes.
- Typography matches the futuristic/arcade theme.

## Must Haves
- [ ] Global CRT scanline overlay.
- [ ] Futuristic font system (Orbitron, Press Start 2P).
- [ ] Non-blocking visual filters.
